pandas==2.3.3
numpy==2.4.2
statsmodels==0.14.6
scipy==1.17.1
plotly==6.5.2
jinja2==3.1.6
typer==0.24.2
pyarrow==24.0.0
platformdirs==4.9.2
python-dotenv==1.2.2
requests==2.32.5
pyyaml==6.0.3
yfinance==1.2.0
mstarpy==11.0.1
pandas-datareader==0.11.1
openpyxl==3.1.5
streamlit==1.52.1
# Install the fundlens package itself from the local source (src/ layout).
# Streamlit Cloud only runs `pip install -r requirements.txt`, so without
# this line the `import fundlens` in app.py fails with ModuleNotFoundError.
.
